development:
  stabilization_dir: <%= Rails.root.join('tmp', 'stabilization') %>
  stabilization_bucket: stabilization-bucket
  ingest_bucket: ingest-bucket
  cul_volume_download_dir: path-to-dir-on-cul
  notification_email: notification-email@email.com
  default_sender_email_address: defaultsender@email.com
  # Currently the stabilization script supports only one source
  sources:
    C:
      username: username@email.copm
      password: pass123456789
      domain: cul
      host: host.cul.columbia.edu
      share: folder-to-access

test:
  stabilization_dir: <%= Rails.root.join('tmp', 'stabilization') %>
  stabilization_bucket: stabilization-bucket
  ingest_bucket: ingest-bucket
  cul_volume_download_dir: path-to-dir-on-cul
  notification_email: notification-email@email.com
  default_sender_email_address: defaultsender@email.com
  # Currently the stabilization script supports only one source
  sources:
    C:
      username: testusername@email.copm
      password: testpass123456789
      domain: cul
      host: testhost.cul.columbia.edu
      share: folder-to-access
